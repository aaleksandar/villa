/**
 * Placeholder for security tests
 * Playwright-based E2E security tests (.spec.ts files) are run separately
 * This file ensures the security test suite can run via Vitest
 */

import { describe, test, expect } from 'vitest'

describe('security tests', () => {
  test('placeholder - E2E security tests will be run separately', () => {
    expect(true).toBe(true)
  })
})
